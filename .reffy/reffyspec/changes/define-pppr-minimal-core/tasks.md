# Tasks

## 1. Spec the boundary

- [ ] Define the required minimal `pppr` harness capabilities and explicit non-goals.
- [ ] Specify how `pppr` relates to `pi` internals: selective reuse is allowed, product-surface inheritance is not required.

## 2. Spec the runtime shape

- [ ] Define the required CLI-first interaction model.
- [ ] Define the required minimal tool contract.
- [ ] Define the required instruction-loading behavior.
- [ ] Define the required observability and session logging expectations.

## 3. Prepare implementation follow-up

- [ ] Identify the core architectural seams that later implementation work must preserve for future embedding.
- [ ] Validate the change so it can serve as the planning baseline for subsequent implementation proposals.
